---
paths:
  - 'app/Livewire/**'
---

# Livewire

## Livewire only for interactive Jetstream surfaces
Build domain pages as controller + Blade; use Livewire class components only where realtime server state is required (e.g. the 2FA setup poll). Keep app.js dependency-free.
